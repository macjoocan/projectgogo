# 게임 데이터 추출·재현 검증 노하우

정리: 2026-09-17. 추출 명령은 [GUIDE.md](GUIDE.md), 현재 인수인계는 [MEMORY.md](MEMORY.md), 시점별 근거는 [HANDOFF.md](HANDOFF.md)를 본다.

이 문서는 작업 과정에서 반복해서 유효했던 판단 기준을 정리한 것이다. 원본 게임 코드·리소스·인증정보를 배포하는 문서가 아니며, 개별 게임의 분석 권한이나 온라인 접근 허용을 다른 게임으로 확대하지 않는다.

## 1. 시작할 때 입력과 작업 범위를 고정한다

- 패키지명, 버전, 원본 파일 SHA256, APK split 목록을 기록한다. 폴더 이름만 보고 게임을 판단하지 않는다.
- 정적 판독, 프로토 진단, 원작 실행, 외부 다운로드를 별도 작업으로 취급한다. 일반 다운로드도 서버 기록이 남을 수 있으며 일회용 환경이 익명성을 보장하지 않는다.
- 원본 파일은 그대로 보존한다. 별도 분석 환경·출력 폴더를 사용하고, 인증/라이선스/보호 기능 우회는 승인된 데이터 분석 작업에 포함시키지 않는다.
- 먼저 기존 결과와 실패 원인을 읽는다. 이미 복구한 데이터 때문에 에뮬레이터나 대용량 추출을 다시 시작하지 않는다.

## 2. 높은 엔트로피만으로 암호화라고 판단하지 않는다

- 선두 바이트뿐 아니라 제한된 헤더 구간에서 압축 시그니처를 확인한다. 작은 사용자 헤더 뒤에 gzip/zlib/LZ4가 들어갈 수 있다.
- magic이 없는 raw 압축도 후보지만, 기대 길이·종료 위치·체크섬·후속 레코드 구조로 검증한다. 디코더가 예외 없이 반환했다는 사실만으로 성공 처리하지 않는다.
- 래퍼 길이, 패딩 길이, 압축 길이, 원문 길이를 구분한다. 네이티브 함수의 입력은 인접한 숫자를 추측하지 말고 실제 호출 인자를 추적한다.
- 타입트리 백엔드/DLL 초기화 실패를 metadata 암호화로 오진하지 않는다. 오류 단계와 파일 자체의 증거를 나눈다.

## 3. 참조와 컬렉션의 정체성을 보존한다

- Unity 객체 키는 `(직렬화 파일, path_id)`다. `m_FileID`를 owner의 externals 기준으로 해석하고 `ObjectIndex`를 사용한다.
- GameObject를 가리키는 필드와 Transform을 가리키는 필드는 다르다. 타입을 확인한 뒤 컴포넌트를 따라간다.
- 스프라이트가 비어 보여도 다른 번들의 Atlas/RenderDataKey를 먼저 확인한다. 이름이 비슷한 이미지로 대체하지 않는다.
- 레벨 번호만으로 덮어쓰거나 중복 제거하지 않는다. 메인/루프/이벤트/변형 컬렉션 경로를 키에 포함한다.
- 레코드 수, 고유 내용 수, 파일 수, 실제 플레이 가능한 수를 따로 보고한다. 로컬 자료 부재와 서버 자료 부재도 다른 주장이다.

## 4. 바이너리는 값뿐 아니라 경계를 검증한다

- 클래스는 namespace·assembly까지 포함해 매칭한다. 동명 클래스의 상속 필드를 섞지 않는다.
- 직렬화 파일 오프셋과 IL2CPP 메모리 오프셋을 구분한다. bool 뒤 정렬, 배열 길이, 문자열 패딩, inline 객체 경계를 확인한다.
- 필드 몇 개가 그럴듯하다는 이유로 성공 판정하지 않는다. 뒤따르는 PPtr 대상과 스키마 끝 위치까지 맞는지 확인한다.
- 저장 기본값, 인스턴스 오버라이드, Config fallback, 실제 실행 선택값을 구분한다. fallback=1은 실행에서도1이었다는 증거가 아니다.
- 심볼 참조/바이트코드 복구를 리터럴 값/원본 텍스트 소스 복구로 표현하지 않는다. 분석을 위해 게임 모듈을 임의 실행하지 않는다.

## 5. 좌표는 부모 Transform 전체를 합성한다

- 메시의 raw 원점, 프리팹 루트 피벗, Mesh 자식 offset, collider center를 따로 기록한다.
- local position을 world position으로 부르지 않는다. 부모 scale·quaternion·translation을 순서대로 반영한다.
- 공통 부모 아래의 형제 오브젝트에는 서로의 offset을 더하지 않는다. 레벨 스폰 +1이 대포에도 적용된다고 추정하면 이중 보정이 된다.
- 메시를 이미 가운데 정렬했는데 높이 절반을 또 더하는지 확인한다. 수정 전 생성 직후(0 step)와 물리 정착 후 상태를 나눠 비교한다.
- 저장 pose와 애니메이션/조준/정착 후 pose를 구분한다. 스크린샷에서 누웠다는 사실만으로 원래 누워서 배치됐다고 단정하지 않는다.
- 가능하면 독립 계산으로 교차 검증한다. 예: 부모 TRS 순차 계산과 Matrix4 합성을 비교한다.

## 6. 셰이더·텍스처는 구현과 선택을 분리한다

- 내보낸 `.shader`가 DummyShaderTextExporter 같은 placeholder인지 먼저 확인한다.
- 컴파일된 프로그램 blob은 길이 prefix·entry 경계·해시를 검증한다. 복구한 GLSL을 원본 HLSL이라고 부르지 않는다.
- 재질→셰이더→패스→키워드→후보 프로그램을 연결한다. 후보 매핑 성공과 실제 런타임 변형 선택 확인은 다른 상태다.
- mask·matcap·base color·lightmap의 역할을 이름이나 평균 밝기만으로 정하지 않는다. 실제 sample 채널/UV/혼합식과 분기를 본다.
- 색공간, saturation 적용 위치, 가산항, 광원 방향 좌표계도 검증한다. 같은 수치여도 연산 순서가 다르면 결과가 달라진다.
- 머티리얼별 셰이더 주입이 다르면 프로그램 캐시 키도 확인한다. 화면이 안 바뀌는 원인이 수치가 아니라 캐시 재사용일 수 있다.

## 7. 물리는 상수가 아니라 소비 경로를 시험한다

- JSON에 값이 존재하는 것과 실제 Rigidbody/Collider에 적용된 것은 다르다. 생성된 엔진 객체에서 mass·damping·radius·gravity·CCD·timestep을 읽는다.
- 오버라이드가 있는 표본과 기본값 경로를 별도 집계한다. 예외 경로가 모두 통과해도 기본값이 틀릴 수 있다.
- 렌더링 dt, 누산기 dt, 물리 solver dt를 각각 확인한다. 30/60/120Hz에서 같은 물리 시간이 흐르는지 시험한다.
- 같은 helper를 공유해도 호출 인자·시점·early return·대상 제외·one-shot 플래그가 다르면 웹과 시뮬은 달라진다.
- 실제 현재 함수를 실행하는 fixture를 쓴다. 함수 이름/시그니처가 바뀌면 옛 진단 스크립트의 추출 범위와 기대값부터 갱신한다.
- 질량 역비례, 직접 타격 대상 제외, 조건부 배율, 저속 첫 접촉, collider 순서 반전, 반복 접촉, 복합 collider를 나눠 시험한다.
- 폭발은 범위 수집, 감쇠 거리, 적용점, 방향, 각속도, 깨우기를 분리한다. 중심 거리 모델의 자기 일관성을 엔진 공식 동작과의 일치로 착각하지 않는다.
- 발사 힘 모드와 충돌 후 폭발 힘 모드는 다를 수 있다. enum은 같은 빌드에서 확인한다.
- 대포 본체·조준 pivot·공 생성점·발사 VFX 위치는 별개다. 수평 속력 고정과 전체 속력 고정도 다른 탄도다.
- 원작과의 차이를 규명하기 전에 레벨별 power로 덮지 않는다. 자체 보정은 `[자체]`, 근사는 `[근사]`로 남긴다.

## 8. 파괴·이펙트·사운드는 이벤트 완료까지 확인한다

- 에셋 존재, 이벤트 연결, 상태 완료(dead/hit/목표 집계), 화면/청음 확인을 각각 검사한다.
- 폭발 플래그만 켜지고 몸체가 제거되지 않는 경우, 연쇄 두 번째 물체의 이펙트 누락, 별칭 중복 호출을 시험한다.
- 파일명에 Broken이 없다고 파괴가 없다고 단정하지 않는다. 온전한 메시를 파편/대체 몸체로 재사용할 수 있다.
- ParticleSystem의 곡선 enum과 원본 mode를 확인한다. 모드 해석 오류를 원본 곡선 부재로 오인하지 않는다.
- 오디오 경로는 Feedback→SfxAsset→AudioClip→실제 PCM을 구분한다. WAV 검증 통과는 올바른 이벤트/볼륨/청음 확인이 아니다.

## 9. 메모리와 재검증 비용을 제한한다

- 큰 번들의 bytes/reader를 무제한 목록에 쌓지 않는다. 파일 하나 또는 제한된 참조 집합 단위로 읽고 해제한다.
- 메모리 측정은 프로세스 트리의 private/commit과 주소 공간 예약을 구분한다. 가상환경 스텁 프로세스만 재면 실제 사용량을 놓친다.
- 선택 의존성은 분석용 경로에 격리한다. 전역 Python을 고치기 전에 실행 파일·버전·모듈 경로를 확인한다.
- 단계별 입력/출력 개수, 실패·스킵·상한 도달 수를 남긴다. 부분 추출이 전체 완료로 보이지 않게 한다.

## 10. 인수인계와 Git은 증거를 보존한다

- 보고서는 주장 / 증거 / 기준 / 공백 / 잔여 위험을 포함한다. 실행 명령, 날짜, 입력 해시, 검사 범위를 남긴다.
- injected event·DOM mock·헤드리스 검증은 실제 물리 접촉 생성·브라우저/GPU·원작 실행 검증과 구분한다.
- 재검증 시작/끝 파일 해시를 비교한다. 다른 세션이 바꾼 파일이면 해당 결과를 최신 코드의 인증으로 쓰지 않는다.
- 우편함에 쓴 것, 상대가 읽은 것, 구현한 것, 다시 검증한 것은 네 단계다. `answered`는 구현 완료 표시가 아니다.
- 최신 요약을 메모리 맨 위에 두고 과거 실패/수정 보고는 날짜가 붙은 이력으로 보존한다. 수정 답신을 받았다고 자동으로 검증 통과로 바꾸지 않는다.
- 명시적 파일 목록으로 stage한다. 원본 패키지·추출물·덤프·개인 임시 경로의 파일·자격증명은 커밋하지 않는다. 기존 미커밋 코드와 문서 정리 커밋을 분리한다.
- 로컬 증거 경로가 Git 밖이면 그 제한을 적는다. 메모리 푸시는 분석 데이터와 재현 환경 전체의 백업이 아니다.
- 푸시 후 원격 branch 해시와 로컬 commit을 다시 비교한다. 실패 시 강제 푸시하거나 타인의 변경을 되돌리지 않는다.

## 작업 재개 체크리스트

1. MEMORY 최신 요약 → HANDOFF 해당 날짜 → 로컬 증거/우편함을 읽는다.
2. 입력 버전·해시와 현재 코드 해시를 확인하고 기존 완료 작업의 반복을 피한다.
3. 원본 저장값 / 원작 실행값 / 프로토 실행값 / 자체 보정 중 무엇을 검증하는지 적는다.
4. 가장 작은 실제 소비자 fixture로 차이를 재현한다.
5. 검증하지 못한 단계는 그대로 남기고, 결과와 전달 상태를 메모리에 갱신한다.
