# 변경 이력

버전 표기는 `levelscope/__init__.py` 의 `__version__` 이 정본이다.
(v1.3까지는 zip 파일명에만 버전이 있고 `__version__` 은 1.0.0에 멈춰 있었다.)

---

## 1.21.0

메모리 폭주 수정. 스프라이트·에셋·계층 추출이 한 프로세스에서 커밋 54GB까지 올라가
PC가 멈추는 일이 세 번 반복됐다(2026-08-14 15:03 · 15:18 · 15:38, 물리 31GB +
페이지파일 38GB = 커밋 한도 약 70GB). 원인은 이미지 처리가 아니라 **Unity 번들 원본
bytes 가 쌓이는 것**이었다. 스프라이트는 PNG를 zip에 바로 쓰고 버리는 스트리밍이라
20,000장이어도 안 쌓인다. 쌓이는 곳이 네 군데였다.

- **`sprites.py` — 의존 번들을 전부 모으던 것을 하나씩으로.** `_dep_bytes` 가 교차 번들
  참조 재시도용 후보를 **개수 상한 없이** bytes 로 리스트에 담고 있었다. Addressables
  게임은 후보가 수십 개고 번들 하나가 90MB인데, UnityPy가 풀면 3~5배로 부풀어 원본과
  해제본이 동시에 살아 있는다. `_dep_names`(이름만) + `_dep_file`(임시파일 경로)로 갈라
  **하나씩** 올리고, 아직 못 푼 오브젝트(파일명+path_id)만 다음 번들로 넘긴다.
  실패 메시지는 시도마다 다시 적히므로 마지막 시도의 것만 남기고, 번들을 하나도 못
  열었으면 원래 실패를 되돌린다 — "없다"와 "못 읽었다"는 다르다.
- **`assets.py` — 후보 개수를 세려고 전부 읽던 것을 이름만.** `_candidates` 가 후보 파일
  bytes 를 전량 dict 에 담아 돌려줬다. 실제로 필요한 건 "후보가 딱 하나인가"뿐이라
  `_candidate_names` 로 경로만 훑고, 하나일 때만 읽는다. 더불어 `ResourceStore` 캐시에
  256MB 상한(`RESOURCE_CACHE_BUDGET`)과 LRU 축출을 뒀다 — Royal Kingdom 은 스트리밍
  리소스가 291개고, 상한 없이 들고 있으면 이 캐시만으로 수 GB 가 된다.
- **`typetree.py` / `hierarchy.py` — 스크립트 색인이 번들을 붙잡던 것을 값만 뽑기로.**
  `ScriptRegistry` 가 MonoScript **reader** 를 담고 있었다. reader 안의
  `assets_file`·`reader` 때문에 번들 env 전체가 색인이 사는 동안 메모리에 남고, 그래서
  호출자는 ExitStack 으로 스크립트 번들들을 런이 끝날 때까지 열어 둬야 했다. 이제
  `add_env` 가 `ScriptRef`(문자열 3개)만 뽑아 두므로 번들을 하나씩 열고 바로 닫는다.
  ExitStack 제거. 색인 내용은 그대로다 — 즉시 읽은 ref 와 지연 읽은 ref 를 7,638건
  비교해 불일치 0을 확인했다.
- **`cli.py` — 다음 단계가 앞 단계 데이터를 붙잡던 것을 해제로.** `run` 이
  sprites/assets/hierarchy 단계 내내 디코딩 결과(`got`·`records`·`result`)를 들고 있었다.
  그 단계들은 레벨 데이터를 쓰지 않는다. 뒤에 필요한 실패·경고 목록만 남기고 놓은 뒤
  `gc.collect()`.

검증 — 산출물이 패치 전과 같은지, 그리고 최고 커밋:

- 스프라이트: RoyalMatch **7,730개** · PixelFlow **1,509개** — 기존 산출물과 정확히 일치.
  최고 커밋 2.0GB
- 계층: Royal Kingdom 씬 3 · 프리팹 1,262 · 노드 **35,867** · MonoBehaviour 복원 89.2% —
  최고 커밋 **3.64 → 3.19GB**
- 에셋: Royal Kingdom **6,574개**(audio 941 · material 913 · font 1 · spine 134 ·
  text 4,585) — 최고 커밋 1.44GB
- 테스트 363개 통과

`configs/*.yaml` 의 `max_count`·`max_nodes` 는 낮추지 않았다. 낮추면 산출물이 실제로
잘려나가고(RoyalMatch 는 `datapack.unity3d` 한 번들에서만 6,286장), 구조를 고친 뒤로는
상한이 병목이 아니다.

주의: `out_royalkingdom` 에 남아 있는 기준치의 복원율 99.5%는 **다른 Royal Kingdom
패키지**에서 나온 값이다. `5251b310….apk`(libil2cpp 80.8MB, Unity 2022.3.39f1)에서는
89.2%가 패치 전후 모두 같은 값이다. 새 코드 탓이 아니다.

## 1.20.0

**Royal Match** (com.dreamgames.royalmatch) 지원. 레벨 14,300개를 슬롯 번호가 아니라
**원래 필드 이름**으로 뽑는다.

- `configs/royalmatch.yaml` · `levelscope/plugins/royalmatch.py` 추가.
  레벨 14,300개 전부 해독(실패 0), 보드 정합성 14,300/14,300.
- **`flatbuffers.names` 배선.** 설정에 이름 지도를 적어도 파이프라인이 무시하고 있었다
  (독스트링에만 있고 구현이 없었다). Royal Match 뷰어가 통째로 0 으로 나와서 잡혔다.
  `flatbuf.set_default_names` / `default_names` 추가, 코덱 스텝이 쓴다.
- **`tools/fbnames_from_dump.py` 추가.** IL2CppDumper 의 dump.cs 에서 FlatBuffers 생성
  클래스(`FTiledLevel`·`FTiledGrid`·`FTiledCell`)를 읽어 이름 지도를 만든다. 벡터 원소
  타입은 프로퍼티가 아니라 **인덱서**(`Cells(int j)`)에 있어서 그것도 본다 — 안 보면
  셀 테이블 이름이 통째로 빠진다.
- **`tools/build_icons_royalmatch.py` 추가.** 스프라이트 이름 규칙 `Items-<이름>Item-<부위>`
  와 `<이름>_Goal_Icon` 으로만 붙인다. `Parts-*` 는 잎사귀·머리 같은 조각이라 안 쓴다.
  여러 칸 기믹은 원본을 행×열로 잘라 칸마다 제 조각을 준다.
- 타일·목표 이름은 앱 열거형에서 온 확정값이다 — `TiledId` 429개, `GoalType` 105개.
  실제로 쓰이는 셀 타입 361종이 **전부** 이름이 붙었다(미상 0).
- Royal Kingdom 의 열거형을 그대로 쓰면 안 된다. 6번이 RK 는 `Match1`, RM 은 `Orange` 라
  전부 어긋난다. 같은 스튜디오라도 열거형은 게임마다 다시 뽑는다.
- 남은 것: 아이콘이 붙은 칸이 아직 일부다(이름이 안 맞는 타일이 310종). 층 배정
  (바텀/커버)은 Royal Kingdom 구조를 따른 **추정**이라 실제 화면 대조가 필요하다.

## 1.19.0

Royal Kingdom 보드를 실제 게임대로 **세 층**으로 되살렸다. 예전 모델은 "바닥 + 덮개"
두 층이라 한 칸에 기믹이 둘이면 하나를 버렸다.

    바텀   타일형 기믹·미션 — 얼음 · 테두리 · 모자이크   (f2.f1 의 f3·f9·f11)
    노멀   상자류 기믹                                  (f2.f0 셀 벡터)
    커버   맨 위, 아래를 다 덮는다 — 담쟁이 · 사슬      (f2.f1 의 f5·f7)

- `layer_cells()` 추가. 칸마다 **목록**으로 돌려주므로 겹친 기믹이 안 사라진다 —
  얼음+담쟁이 663칸, 얼음+사슬 397칸, 얼음+테두리 352칸이 그렇게 묻혀 있었다.
  전체 46,698칸(예전 평면 집계 44,879)으로 1,819칸이 되살아났다.
- 뷰어는 바텀 → 노멀 → 커버 순으로 쌓는다. 같은 층에 기믹이 둘이면 층을 더 만든다.
  모달 칩도 "커버"·"바텀"으로 나눠 보여준다.
- xlsx 에 `bottom_cells`·`bottom_kinds`·`bottoms` 컬럼 추가.
- 모르는 슬롯에 기믹 값이 있으면 커버로 올린다 — 묻어두지 않는다.
- 층 배정 근거: 9스테이지 실제 화면에서 아래 세 줄이 **나무 상자**로 보이는데 그 칸은
  노멀 Box2 + 얼음이다. 상자가 보이니 얼음이 아래다. 담쟁이는 7스테이지에서 칸을
  통째로 가리므로 위다. Lv9 가 화면 목표(상자 27 · 얼음 27 · 담쟁이 36)와 정확히 일치.
- `tests/test_plugin_royalkingdom.py` 신설 (12개).

## 1.18.0

다중칸 기믹 아이콘을 제대로 그린다. 44스테이지 실제 화면과 대조해 잡았다.

- **`tools/build_icons_royalkingdom.py` 추가.** 열거형 이름 끝의 칸 위치(앞=행 T/M/B,
  뒤=열 L/M/R)를 읽어 원본을 행×열로 자르고 각 칸에 제 조각을 준다. 예전엔 칸마다
  원본 **전체**를 그려서 2×2 기믹 하나가 화면에 4개로 보였다.
- 12종 70개 조각 생성 — ChaliceBox·MagicOrb·DragonBox·JellyMaker·MaceBox·MagicCards·
  Mushrooms(Close/Open)·Snowman·Spaceship·TurtleHouse·Armory.
- `MagicOrb` 는 열거형 이름이 갈려 있다(커튼 세 칸 `MagicOrbCurtain*` + 한 칸
  `<색>MagicOrbBL`). 한 자리에 타입이 여럿이라 **목록으로 받는다** — 처음엔 자리당
  하나만 받아 색 변형 9개가 조용히 빠졌다.
- 원본을 못 찾은 Clock·Knight·Lightbulb·SteamBomb 는 **아이콘을 만들지 않는다.**
  뷰어가 타입 번호를 그대로 보여주므로 "그림이 없다"가 드러난다.
- 검증: Lv44 가 화면과 일치 — 이동 31, MagicOrb 계열 16칸 == 목표 "무지개 구슬 16".
- 남은 범위: 전체 448,889칸 중 7.44%(33,418칸)가 아직 번호로 나온다.

## 1.17.0

FlatBuffers 해독에서 **스칼라가 벡터로 둔갑하던 두 경로**를 막았다. Royal Kingdom
9스테이지 실제 화면(상자 27 · 얼음 27 · 담쟁이 36 · 이동 30)과 대조해 잡은 버그다.

- **uint8 벡터 폴백 제거** (`flatbuf._scalar_vector`). int32 로 안 보이면 이제 벡터가
  아니라고 본다. 예전엔 아무 쓰레기나 u8 벡터로 받아줘서 진짜 스칼라를 덮어썼다 —
  Lv9 의 얼음 27칸 중 **25칸이 그렇게 사라졌다.**
- **빈 벡터는 타입 투표에서 뺀다** (`flatbuf.infer_schema`). 목록이 비어 있다는 사실은
  그 슬롯이 스칼라라는 근거가 못 된다. 동시에 스키마가 확정하기 전에는 빈 벡터를
  받지 않는다 — 0으로 채워진 자리를 가리키는 스칼라가 통째로 "빈 벡터"가 됐었다.
- 검증: 보드 정합성 4,500/4,500 유지, f3 목록 447개 유지, Lv7 담쟁이 72 유지,
  Lv9 가 화면 목표와 **정확히** 일치(27·27·36·이동 30). 테스트 348개 통과.

Royal Kingdom 아이콘 4종을 실제 원본으로 교체했다. 사용자가 6·9·12스테이지에서
"처음 보는 기믹"으로 지목한 것들이다.

- `Box1~3` 남색 사각형 → **`Items-BoxItem-box`** (나무 상자)
- `Ice1~2` 하늘 그라데이션 띠 → **`Ice_big`** (연한 파랑 사각)
- `Chain1~2` C자 파편 → **`Chain02`** (고리 한 개)
- `Border` 는 원본이 28x1 선뿐이라 **아이콘을 지우고 미매핑으로 뒀다.** 잘못된 그림을
  붙이는 건 안 붙이는 것보다 나쁘다.

## 1.16.0 — 2026-08-14

**Fill = 빈 통로로 확정.** 실제 게임 화면으로 검증했다.

### 검증 — Lv151

제보된 화면(목표 "과녁 72", 이동 25)이 데이터와 정확히 맞았다.

    과녁(TargetA 9 + TargetB 48 + TargetC 15) = 72   ← 화면 목표 72
    이동 25                                          ← 화면 Moves 25

격자도 화면과 그대로 겹친다.

       0~2행  과녁 3줄
       3행    None      ← 화면의 위쪽 빈 틈
       4~6행  좌우 과녁 + 가운데 조각
       7행    Fill      ← 화면의 아래쪽 빈 틈
       8~10행 과녁 3줄

**화면에서 3행(None)과 7행(Fill)이 똑같은 빈 틈으로 보인다.** Fill 은 칸은 있지만
아무것도 그려지지 않는 통로였다. 장애물이 아니다.

앞서 구조 분석에서 나온 "Fill 5,827칸 중 85%가 열 맨 아래"도 이와 맞는다 —
조각이 빠져 내려가는 자리다.

### 반영

- 뷰어에서 Fill 계열은 **타일을 그리지 않는다** (None 과 같이 빈 칸)
- 기믹 집계에서 제외하고 `fill_cells` 컬럼으로 따로 센다
- 잘못 붙였던 Fill 아이콘 4종 삭제
- 과녁 아이콘을 화면과 같은 빨강/흰 원형(`target_goal`)으로 교체

v1.15.0 에서 "Fill 은 기믹인데 아트를 못 찾았다"고 적었던 건 틀렸다. 기믹이 아니라
빈 칸이라 아트가 없는 게 맞았다.

---

## 1.15.0 — 2026-08-14

**커버형 기믹(덮개) 복원.** 보드가 두 층이라는 걸 놓치고 있었다. 실제 게임 화면
제보로 드러났다.

### 무엇을 놓쳤나

`f2.f0` 를 보드로 읽고 `f2.f1` 은 "대부분 빈 테이블"이라며 넘겼다. 그게 아니었다 —
**`f2.f1` 이 셀 위에 덮인 것**이고, 커버형 기믹이 전부 거기 있다.
**4,500개 전 레벨에 있고 합계 130,785칸**을 통째로 빼먹고 있었다.

그래서 7스테이지가 뷰어에서 밋밋한 회색 판으로 나왔다. 실제로는 담쟁이가 판을
덮고 있는 레벨이다.

### 검증

제보된 7스테이지 실제 화면의 목표가 **"담쟁이 72"** 인데, Lv7 의 `f2.f1` 에서
비어있지 않은 칸이 **정확히 72개**이고 값이 전부 `Ivy1`(34)이다. 이동 수도 30으로
화면과 같다.

### 덮개 종류

슬롯마다 다른 덮개가 들어간다.

| 슬롯 | 덮개 | 칸 수 |
|---|---|---|
| `f5` | Ivy1 (담쟁이) | 11,627 |
| `f9` | Border (테두리) | 9,805 |
| `f3` | Ice1 / Ice2 (얼음) | 7,589 |
| `f11` | Mosaic / Puzzle | 3,788 |
| `f7` | Chain1 / Chain2 (사슬) | 3,244 |

`f0`·`f6`·`f10`·`f12` 는 값이 1~5(색)이라 덮개가 아니라 미리 놓인 조각이다.

### 반영

- 뷰어에서 덮개를 **바닥 위 레이어로 렌더** — 슬라이더로 벗겨 바닥을 볼 수 있다
- `cover_cells` · `cover_kinds` · `covers` 컬럼, 모달에 "덮개" 칩 섹션
- 기믹 집계에 덮개 포함 (Lv7: 기믹종류 1 → 5, 기믹칸 27 → 99)
- 담쟁이·얼음·사슬·테두리·모자이크 아이콘 추가

### 같이 정정

`Fill` 계열(11·17·18·19)을 판 구성에서 빼 기믹으로 되돌렸다. 배치를 보면 성격이
다르다 — `Set1` 은 판 전체를 채우는데(Lv2801: 99칸 중 94칸) `Fill` 은 대칭 블록으로
놓인다(Lv2878: `###ggg###`). 다만 `Fill` 전용 아트는 아직 못 찾아 다른 타일로
구분만 해 두었다.

---

## 1.14.0 — 2026-08-14

**전투 퍼즐 레벨 구분.** 레벨 번호 끝자리가 5·0(5의 배수)이면 일반 매치3가 아니라
전투 퍼즐이라는 제보를 데이터로 확인하고 반영했다.

### 확인

전투 전용 슬롯 `f7` 보유 레벨과 5의 배수가 **900개로 완전히 일치**했다(불일치 0).
성격도 확연히 다르다.

    5의 배수   셀수  62.0 · 기믹종류 0.77
    나머지     셀수 103.3 · 기믹종류 8.35

`f7.f1` 이 단계(웨이브) 벡터이고 원소마다 자기 보드를 갖는다 — 1~3단계, 9×10 또는
8×10. 셀수 = 가로×세로 정합성이 **1,748개 보드 전부**에서 맞는다.

### 전투 보드는 ID 공간이 다르다

IL2CPP 메타데이터에 `TiledId` 와 별개로 **`AttackTiledId`**(220 멤버)가 있다.
성(Tower)·문(Gate)·골렘·문어·두더지·보스 같은 전투 요소다. 같은 번호라도 뜻이
다르므로(24 = 일반 `Safe` / 전투 `TowerBL`) 뷰어에서 `BATTLE_ID_OFFSET`(100000)만큼
띄워 아이콘을 구분한다.

### 반영

- `mode`(전투/일반) · `battle_waves` · `battle_grid` · `battle_kinds` · `battle_units` 컬럼
- 뷰어 상단 **전투/일반 필터**, 카드에 전투 배지
- 전투 단계를 **레이어로 렌더** — 모달 슬라이더로 단계를 벗겨 볼 수 있다
- 모달 칩에 단계별 전투 요소 이미지 + 칸 수
- 전투 아이콘 177종 추출, 전투 셀 커버리지 **96.4%**

전투 유닛은 통짜 아이콘이 드물고 부품 스프라이트(`golem_particle_*`,
`scarecrow_hat`)로 흩어져 있다. 대표 이미지가 있는 것만 직접 지정했고, 그렇게
붙였다는 사실을 설정 `notes` 에 남겼다.

---

## 1.13.0 — 2026-08-14

**Royal Kingdom 뷰어를 전체 스프라이트 기준으로 갱신.** 보드에서 숫자가 사라지고
모든 칸이 게임 원본 아트로 그려진다.

v1.12.0에서 스프라이트 6,826개를 온전히 뽑았으므로, 그 세트를 기준으로 기믹 매칭을
다시 만들었다. 아이콘 **46종 → 113종**, 셀 커버리지 **75% → 92.7%**.

### 매칭을 세 번 고쳤다

1. **이름 자동 매칭만으로는 오히려 나빠졌다** (46종 → 32종, 커버리지 39.9%).
   길이 페널티와 임계값이 과해서 `Safe`→`Parts-safe01`, `ColumnDown`→
   `Items-Column-Column_Down` 같은 정답이 탈락했다. 검증된 기존 매핑은 두고
   **빠진 것만** 채우는 방식으로 바꿨다.
2. **판 구성 마커에 억지 이미지가 붙었다.** `Match1~4` 가 전부 같은
   `board_match_items` 로, `Set2` 는 `settings_icon_rj` 로 매칭됐다. 이들은 장애물이
   아니라 "이 칸은 이런 성격" 표시라 전용 아트가 없다. 13종을 **같은 빈 칸 타일**로
   통일했다 — 없는 구분을 이미지로 만들어내지 않기 위해서다.
3. **2×2 조각 기믹을 놓치고 있었다.** `ChaliceBoxTL/TR/BL/BR` 처럼 네 조각으로 나뉜
   기믹은 이름이 안 맞는다. 모서리 접미(TL·TR·BL·BR·TM·BM·ML·MR·MM)를 떼고
   다시 찾아 **44종**을 더 붙였다.

### 기믹 판정 정정

`Match1~4` · `Fill1~3` · `Set1~3` 을 기믹 집계에서 뺐다. 매치 가능한 조각과 칸 성격
표시를 "기믹(장애물)"으로 세면 수치가 부풀려진다. 예: Lv9 는 `기믹종류 4 · 기믹칸 34`
(Box2, Match2, Match3, Match4) 였는데 이제 `기믹종류 1 · 기믹칸 27` (Box2) 이다.

산출물: `RoyalKingdom_viewer.html` 22.7MB (아이콘 113종 임베딩).

---

## 1.12.0 — 2026-08-14

**스프라이트 교차 번들 참조 해결.** 텍스처가 다른 번들에 있는 스프라이트가 추출에서
빠지고 있었다.

- Royal Kingdom: `datapack.unity3d` 의 스프라이트 77개가 `sharedassets0.assets`(다른
  번들 `data.unity3d` 안)를 참조해 `FileNotFoundError` 로 실패했다.
- Zen Match: `zenmaster_shared` 의 14개가 `cab-402470…`(다른 번들)을 참조해 실패했다.

`UnityPy.load()` 는 여러 파일을 한 환경에 올릴 수 있다. **실패가 났을 때만** 참조 대상
번들을 같이 올려 그 오브젝트만 재시도한다 — 실패가 없으면 비용이 0이다.

    Royal Kingdom  6,749 → 6,826  (실패 77 → 0)
    Zen Match      3,786 → 3,800  (실패 14 → 0)

찾는 방식도 고쳤다. 처음엔 "sharedassets 를 가진 번들"이라는 어림짐작으로 골랐는데,
그러면 이름이 `CAB-<hash>` 인 경우를 놓치고 큰 번들은 크기 상한에 걸려 빠졌다.
**실패 메시지가 못 찾은 파일 이름을 그대로 알려주므로** 그걸로 찾는다.

재시도 대상도 이름이 아니라 **오브젝트 id(파일명+path_id)** 로 집는다. 이름으로
거르면 이름이 같은 다른 스프라이트를 조용히 흘린다 (첫 구현에서 실제로 그랬다 —
Zen Match 14개가 "실패 0, 추가 0" 으로 사라졌다).

### 산출물 정리

리소스가 임시 폴더에만 있던 것을 `D:\99.기타\out_<게임>\` 으로 모았다.
`out_zenmatch\` 를 새로 만들었다.

---

## 1.11.0 — 2026-08-14

**기믹 이름 확정 + 원본 이미지 렌더.** 뷰어가 셀을 색 사각형이 아니라 게임 원본
아트로 그리고, 기믹마다 실제 이름이 붙는다.

### 셀 타입 → 기믹 이름 (확정)

IL2CPP 메타데이터에서 `TiledId` 열거형(220 멤버)을 복원했다. 4,500 레벨에 실제로
쓰인 **178종이 전부 이 표 안에 있다 — 미상 0.**

    0 None · 1~5 Blue/Green/Red/Yellow/Pink · 6~9 Match1~4 · 14 Set1
    21 Box1 · 24 Safe · 26 Vase · 36 Lantern · 46 ColumnDown · 90 Log
    95 Magnet · 98 Lily · 140 GiftBox · 157 PuzzleBox · 163 Flowers …

이제 xlsx·뷰어가 "타입 26" 대신 **"Vase"** 로 보여준다. `gimmick_kinds`(기믹 종류
수)·`gimmick_cells`(기믹 칸 수)·`gimmicks`(주요 기믹 이름) 컬럼이 추가됐다.

### 뷰어 — 원본 기믹 이미지

`render: tiles` 로 전환하고 게임 스프라이트 46종을 뽑아 `configs/icons/royalkingdom/`
에 넣었다. 카드·모달 보드가 실제 매치 피스와 기믹 아트로 렌더되고, 모달 하단
**기믹 구성** 칩이 기믹 이미지 + 칸 수로 나온다. 셀 커버리지 약 75%.

이미지가 없는 타입은 번호로 표시된다 — 이름은 확정값이지만 **이미지 매핑은 이름으로
찾아 붙인 것**이라 그 구분을 유지했다.

### f3 — 구조 복원, 의미는 미확정

`f3` 를 읽으려면 빈 벡터 처리가 먼저 필요했다. `vec_len` 이 길이 0을 거부해서
"목표 없는 레벨"의 빈 목록이 통째로 해석 불가가 됐고, 다수결이 그 슬롯을 스칼라로
잘못 확정하고 있었다. 고친 뒤 드러난 것:

- 4,500개 중 **447개(10%)** 에만 값이 있다
- `{f0, f1, f2}` 구조 · f0 ∈ 1~8, f1 ∈ 10~111, f2 = 정수 벡터
- (f0, f1) 이 격자를 이룬다 (447개 중 281개는 빈칸 없는 완전격자)
- f2 값은 셀 타입과 **같은 ID 공간**을 쓴다

의미를 좁히려 한 검사 두 개는 **결론을 내지 못했다** — f2 값이 그 레벨 보드 타입의
부분집합인지는 211 예 / 236 아니오로 갈렸고, f3 유무와 난이도는 무관했다
(이동수 26.3 vs 26.7). 목표(goal)라면 모든 레벨에 있어야 하는데 10%뿐이라 목표는
아니다. 그 이상은 게임을 돌려 대조하기 전엔 단정할 수 없어 **미확정으로 남긴다.**
형태만 `f3_entries` · `f3_f0_max` · `f3_f1_span` 컬럼으로 노출한다.

### 같이 고친 것

- **`vec_len` 이 빈 벡터(길이 0)를 거부했다.** 위 f3 문제의 원인. 빈 벡터는 정상이며,
  대신 빈 문자열은 문자열로 보지 않는다(빈 벡터의 길이 필드가 죄다 "빈 문자열"로
  읽혀 진짜 벡터를 가리기 때문).
- 스프라이트 추출 시 **path_id 만으로 오브젝트를 찾으면 안 된다** — 한 번들 안
  여러 SerializedFile이 같은 path_id를 쓴다. 실제로 Sprite 대신 Material이 잡혔다.
  `(파일명, path_id)` 로 키를 잡아야 한다.

### 알려진 한계 (v1.12.0에서 해결됨)

일부 스프라이트는 텍스처가 다른 SerializedFile에 있어 UnityPy가 해석하지 못했다
(`Items-MatchItem-*` 계열). 아이콘 추출 시에는 렌더 가능한 대체 스프라이트를 찾아 썼고,
`sprites` 명령의 교차 파일 참조 실패는 **v1.12.0 에서 해결**했다.

---

## 1.10.0 — 2026-08-14

**FlatBuffers 레벨 복원.** v1.9.0에서 "읽을 수 없다"고 보고했던 Royal Kingdom 레벨
4,500개를 실제로 복원했다.

### flatbuf — 스키마 없는 FlatBuffers 해독기 (새 모듈)

`.fbs` 스키마가 없어도 구조는 되살릴 수 있다. FlatBuffers는 테이블마다 vtable
(필드 슬롯 → 테이블 내 오프셋)을 들고 다니기 때문이다. 문제는 **타입이 안 적혀
있다**는 것 — 4바이트 값이 정수인지 다른 객체를 가리키는 오프셋인지 버퍼만 봐서는
모른다. 세 단계로 좁혔다.

1. **널 종료자** — FlatBuffers 문자열은 길이 뒤에 `\0` 이 붙는다. 이걸 요구하니
   "값 16이 길이 16짜리 문자열"로 읽히던 오독이 사라졌다.
2. **4바이트 정렬** — 길이 필드가 uint32라 오프셋 대상은 항상 4바이트 정렬이다.
   이 검사 하나로 **1,462개** 오독이 걷혔다 (보드 높이 7이 "길이 256 벡터"로 둔갑).
3. **표본 다수결** — 그래도 정렬을 우연히 만족하는 값이 남는다(683개). 같은 스키마의
   버퍼가 4,500개 있으니 슬롯별로 다수결하면 확정된다. `infer_schema()` 가 그 일을 한다.

결과: **보드 정합성(셀 수 == 가로×세로) 4,500 / 4,500, 위반 0.**

### 파이프라인 통합

- `codec: [flatbuffers]` 스텝 추가. 종결 스텝이다.
- 코덱 스텝은 버퍼를 하나씩 받아 표본을 못 모으므로, 파이프라인이 **디코딩 전에**
  전체 표본으로 스키마를 확정해 걸어 둔다(`_prepare_flatbuffers`). 런이 끝나면 지운다.
- `configs/royalkingdom.yaml` + `plugins/royalkingdom.py` 추가.

### 같이 고친 것

- **바이너리 종결 스텝에서 zip에 원본 바이너리가 들어갔다.** `levels/1.json` 안에
  FlatBuffers 바이너리가 그대로 들어앉아 열리지 않았다. 종결 스텝이 바이너리면
  디코딩 결과를 JSON으로 낸다(msgpack도 같이 해결).

### 복원된 것 (Royal Kingdom 4,500 레벨)

| 필드 | 내용 | 근거 |
|---|---|---|
| `f0` | 레벨명 `0001v01_E` … `4500v01_E` | 파일 번호와 1:1 일치 |
| `f1` | 이동 수 (22~31 분포) | 분포 형태 — **추정** |
| `f2.f0` | 셀 타입 int32 배열 | 길이가 항상 `f2.f2 × f2.f3` |
| `f2.f2/f3` | 가로 / 세로 | 8~9 × 7~67 |
| `f3` | 목표·기믹 추정 테이블 벡터 | **미확정** |

보드 크기 분포: 9×11 (2,055) · 9×7 (773) · 9×10 (730) · 8×11 (370) · 9×9 (157) ·
9×38 (40) 등. 세로 37~67짜리는 스크롤 스테이지로 보인다.

산출물: xlsx 4,500행 × 11열 · 뷰어 HTML(4,500레벨 보드 렌더) · 디코딩 JSON zip.
뷰어는 브라우저에서 콘솔 에러 없이 렌더된다.

필드 이름은 스키마에만 있으므로 슬롯 번호(`f0`…)로 나온다. 의미 매핑은 근거와 함께
`plugins/royalkingdom.py` 주석과 설정 `notes` 에 적었고, **추정과 확정을 구분**했다.

테스트 324 → 347개.

---

## 1.9.0 — 2026-08-14

네 번째 게임(Royal Kingdom, `gg.com.dreamgames.royalkingdom`)으로 시험하다 나온
결함들을 고쳤다. 앞의 세 게임에서는 드러나지 않던 구조라 전부 새로 발견된 것이다.

이 APK가 까다로웠던 이유:
- **APKS 래퍼** — 바깥 apk 안에 `assets/apks/*.apk`(base·config·**UnityDataAssetPack**)가 들어 있다
- **확장자 없는 번들** — Play Asset Delivery 팩에 `assets/android/gameplay/hard_level/ui_v2` 같은
  이름의 번들이 114개 있다
- **번들 내부 오디오** — 각 번들이 자기 사운드를 `CAB-<hash>.resource` 로 안에 품는다
- **스크립트/사용처 분리** — 스크립트 정의는 `data.unity3d`(3.8MB), 그걸 쓰는 컴포넌트는
  `datapack.unity3d`(88MB)에 있다

### 고친 것

- **확장자 없는 Unity 번들 114개를 통째로 놓쳤다.** 후보 판정이 이름(확장자·경로 힌트)만
  봤기 때문이다. 이제 이름으로 **확실한 것만 걸러내고** 앞 32바이트의 매직(`UnityFS`)을
  본다. 소스 발견 **3개 → 117개**, 오브젝트 117,429 → 144,048.
  `Container.head()`·`size_of()` 를 추가해 후보 판정 전에 엔트리를 통째로 읽지 않으므로
  오히려 빨라졌다(269MB APK 기준 4초).

- **`survey` 가 실제 레벨 계열을 못 봤다.** 페이로드 큰 순으로 앞 400개만 검사했는데,
  그 400개가 전부 Spine `.skel`(최대 517KB)이라 정작 레벨인 `1~4500` 계열 4,500개가
  표본에 들지도 못했다. 이제 **계열 단위로 세고** 계열마다 표본을 검사한다.
  덩치는 판별 기준이 못 된다.

- **디코딩 안 되는 계열을 침묵으로 넘겼다.** "레벨 후보 1개"만 보여주면 레벨이 없는
  게임처럼 읽힌다. 이제 못 읽는 계열도 개수·중간 크기·머리 바이트와 함께 보고한다.
  Royal Kingdom에서 `[0-9]+ ×4500 · 2,284B · 머리 2400000000001e00` 이 바로 드러나고,
  `reactivation_[0-9]+ ×35` 가 **같은 시그니처**라 동일 포맷임까지 보인다.

- **번들 내부 오디오 291개가 엉뚱한 파일에서 잘려 나왔다.** `CAB-<hash>.resource` 를
  못 찾자 "후보가 하나뿐"이라며 `assets/bin/Data/resources.resource` 로 대체했는데,
  오프셋은 원래 파일 기준이라 결과가 깨진다 — v1.7.0에서 막으려던 바로 그 실패를
  대체 규칙이 뒷문으로 다시 열어준 셈이다. 이제 **번들 내부를 먼저 찾고**,
  `CAB-` 로 시작하는 이름은 대체를 아예 하지 않는다.
  오디오 실패 **291 → 0** (941개 전부 WAV, `.fsb` 폴백 0건).

- **컴포넌트 필드 복원률이 14%에 그쳤다.** 스크립트 정의와 사용처가 다른 번들에 있어
  UnityPy의 `deref()` 가 상대 파일을 못 찾았다(스크립트참조없음 8,693개).
  `typetree.ScriptRegistry` 를 추가해 **스크립트 전용 번들만** 미리 열어 색인해 두고
  번들 경계를 넘어 찾아간다. **14.0% → 99.5%** (10,110/10,158, 참조없음 2).
  조건을 "MonoScript 100개 이상이고 MonoBehaviour 보다 많은 번들"로 좁혀,
  Zen Match의 90MB 번들을 두 번 읽는 일은 생기지 않는다.

- **음수 `m_FileID` 에서 터졌다.** 위쪽 범위만 막아서 파이썬 음수 인덱싱에 걸렸고
  `IndexError` 로 런이 중단됐다. 양쪽을 다 막는다.

### Royal Kingdom 실측 (v1.9.0)

| 항목 | 값 |
|---|---|
| Unity / 백엔드 | 2022.3.39f1-d1.8 · IL2CPP |
| 자동 발견 소스 | 117개 (확장자 없는 번들 114 포함) |
| 리소스 추출 | 1,989개 (오디오 941 · 머티리얼 913 · Spine 134 · 폰트 1), 실패 0 |
| 씬 / 프리팹 / 노드 | 3 / 1,262 / 35,867 (128초) |
| 컴포넌트 필드 복원 | 10,110 / 10,158 (99.5%) |
| 레벨 데이터 | `[0-9]+` ×4,500 — 커스텀 바이너리(FlatBuffers 형태), **디코딩 미지원** |

레벨 4,500개는 압축·base64가 아니라 스키마가 필요한 바이너리라 지금은 못 읽는다.
survey 가 그 사실과 근거(머리 바이트·계열 크기)를 보고하는 데까지가 현재 범위다.

테스트 310 → 324개.

---

## 1.8.0 — 2026-08-13

게임별 설정에 묶여 있던 부분을 걷어내 **아무 Unity 모바일 게임에나 바로 쓸 수 있게** 했다.

- **`discover` — Unity 소스 자동 발견** (새 모듈).
  전에는 `sources` 를 사람이 적어야 했고 기본값이 `assets/bin/Data/data.unity3d`
  하나였다. 그래서 Addressables를 쓰는 게임은 `assets/aa/Android/*.bundle` 을
  **통째로 놓쳤다** (Zen Match 15개 7.5MB, PixelFlow 24개). 이제 컨테이너를 훑어
  Unity로 열리는 것을 전부 찾는다. `sources` 를 비우거나 `auto` 로 두면 적용된다.

  함정이 하나 있었다. **`UnityPy.load()` 는 아무 파일에나 성공한다** — JSON이든
  boot.config든 오브젝트 0개인 env를 준다. "열리면 Unity"로 잡으면 조용히 쓰레기를
  훑게 되므로 `objects > 0` 을 본다. 반대로 `unity default resources` 처럼
  **확장자가 없는** SerializedFile(오브젝트 85개)도 있어서 확장자만으로도 안 된다.

  Unity 내장 리소스(기본 셰이더·폰트)는 발견은 하되 추출 기본값에서 뺀다. 모든
  게임에서 같은 내장 에셋이 산출물에 섞이면 "이 게임 리소스"를 보는 데 방해가 된다.

- **`survey` — 처음 보는 APK 프로파일** (새 모듈, 새 CLI 명령).
  새 게임 온보딩의 첫 단계였던 `ls`→`detect`→`inspect`→YAML 손작업 앞에, "이 게임이
  Unity인가·IL2CPP인가·Addressables를 쓰는가·리소스가 얼마나 있나·레벨 후보가 뭔가"를
  한 번에 보고하는 단계를 놓았다. `--configs` 를 주면 설정 초안까지 쓴다.

  레벨 후보 판정에 두 가지를 넣었다. **디코딩되는 것만** 후보로 올린다 (안 그러면
  Spine atlas가 앞자리를 먹고 실제 레벨 JSON이 밀려난다 — SheepNSheep에서 그랬다).
  그리고 이름을 **계열**로 묶어 멤버 수로 순위를 낸다 — 제일 큰 TextAsset은 보통
  설정 덩어리(`ZenMasterConfigDataModel`)고 레벨은 번호 붙은 큰 계열(`Level_[0-9]+`
  ×377)을 이루기 때문이다. 크기순으로 뽑으면 엉뚱한 걸 집는다.

  레벨이 Unity 에셋이 아니라 **파일**로 든 게임(PixelFlow: `assets/Levels/*/*.json`
  2,384개)도 함께 찾는다. 양쪽에 후보가 나오는 게임이 있어서(Zen Match는 에셋
  레벨 377 + 미니게임 파일 7) 규모 차이로 가른다.

  검증: 세 게임 모두 초안의 `name_pattern`·`levels_glob`·`codec` 이 사람이 쓴 설정과
  일치했고, PixelFlow는 생성된 초안을 **손대지 않고** 돌려 레벨 2,384개를 찾아
  30개 디코딩(실패 0)에 성공했다.

- **`catalog` — Addressables 카탈로그 해독** (새 모듈).
  번들 이름이 `room10__a259fe9091a05b92773fd5c141a7db0a.bundle` 이라 추출물만 봐서는
  뭔지 알 수 없다. `catalog.json` 을 풀어 원본 프로젝트 경로로 되돌린다
  (`Assets/Modules/ThemeModule/.../Bamboo_tiny.png`). 함정 두 개: 키 오프셋 표는
  `m_KeyDataString` 이 아니라 `m_BucketDataString` 에 있고, 에셋이 든 번들은
  엔트리의 `dependencyKeyIndex` 를 따라가야 나온다.

  해독 결과는 검증한다 — 버킷 파싱이 블롭을 정확히 소진하는지, 엔트리 블롭 크기가
  `4 + count*28` 과 맞는지, 인덱스가 범위 안인지. 하나라도 어긋나면 해독을 포기하고
  `m_InternalIds` 목록만 쓴다. **잘못 푼 매핑으로 이름을 붙이는 건 이름을 안 붙이는
  것보다 나쁘다.** `catalog.bin`(바이너리 전용 포맷)은 문자열만 건지고 그 사실을 밝힌다.

  부수 효과로 유용한 게 나왔다 — Zen Match는 카탈로그 번들 389개 중 **375개가 APK에
  없다**(원격 CDN 배포분). 추출 대상이 아니라는 걸 survey가 알려준다.

### 같이 고친 것

- **CLI `--source` 기본값이 하드코딩 경로여서 자동 발견이 CLI에서 아예 동작하지
  않았다.** `sprites`/`assets`/`hierarchy` 셋 다 그랬다. 기본값을 `auto` 로 바꿨다.
  이걸 잡기 전 Zen Match `assets` 는 Addressables 번들 14개를 건드리지도 못했다
  (수정 후 오디오 84→103, 폰트 4→11, 번들 기여 27개).
- **상한(`max_count`)에 걸려 아예 열지도 못한 소스를 알려주지 않았다.** Zen Match에서
  `data.unity3d` 의 TextAsset 3,486개가 상한을 다 먹어 번들 14개는 손도 못 댔는데
  로그는 "중단"만 말했다 — 사용자는 그 번들이 비어 있었다고 오해한다. 이제 못 연
  소스를 이름까지 짚고 해결책(`kinds` 에서 `text` 빼기)을 안내한다.
- **`discover.probe` 에서 부수 정보의 실패가 판정 전체를 무효로 만들었다.**
  원시 SerializedFile은 내부 파일 키가 int(path_id)라 문자열 취급이 터졌고, 그 예외가
  오브젝트 85개를 멀쩡히 읽어낸 결과까지 버렸다. 부수 계산을 분리했다.
- **`hierarchy` 가 소스마다 `MonoReader` 를 새로 만들었다.** 소스가 하나일 때는 안 보였던
  낭비다 — Zen Match는 소스가 16개라 `libil2cpp.so`(112MB) + `global-metadata.dat`(20MB)를
  백엔드마다 다시 읽어 그 로딩만 100초를 넘겼다. 리더 하나를 공유하도록 바꿔 57초가 됐다
  (한 빌드 안에서는 Unity 버전·스크립트 정의가 같으므로 공유해도 된다).

테스트 241 → 310개.

---

## 1.7.0 — 2026-08-13

레벨 밖 리소스까지 추출 범위를 넓혔다. 이전에는 `sprites` 하나뿐이었고 그 구현이
`obj.image` → PNG 한 갈래여서, 이미지가 없는 타입은 전부 "이미지 없음" 실패로 떨어졌다.

- **`typetree` — IL2CPP MonoBehaviour 필드 복원** (새 모듈).
  IL2CPP 빌드는 스크립트의 필드 구조를 번들에 담지 않아 UnityPy가 MonoBehaviour를
  거의 못 읽는다 (SheepNSheep 1659개 중 1653개 실패). `libil2cpp.so` +
  `global-metadata.dat` 를 TypeTreeGeneratorAPI에 먹여 구조를 되살린다.
  **IL2CppDumper를 돌릴 필요가 없다** — 재료를 APK에서 바로 읽어 2.5초에 끝난다.
  백엔드는 순서대로 폴백한다. 하나로는 부족하다는 게 실측으로 나왔다:

      AssetRipper 단독      1364/1659 (82.2%)
      + AssetStudio 폴백    1659/1659 (100%)

  못 읽는 쪽이 `I2.Loc.Localize`(200개)·UI LayoutGroup 계열(52개)·Spine 계열(33개)로
  뚜렷하게 갈리므로, 클래스별로 성공한 백엔드를 기억해 재시도 낭비를 없앴다.
  재료가 없거나 패키지가 없으면 `NullMonoReader`로 내려앉아 런을 죽이지 않는다.

- **`assets` — 사운드·머티리얼·폰트·Spine·텍스트 추출** (새 모듈, 새 CLI 명령).
  가장 손이 많이 간 건 오디오다. AudioClip의 소리 데이터는 번들 안에 없고
  `m_Resource` 가 **번들 밖** `resources.resource` 의 오프셋을 가리킨다. UnityPy는
  그 파일을 못 찾아 빈 바이트로 디코딩해 `FmodError('FORMAT')` 을 냈다. 컨테이너에서
  직접 찾아 오프셋만큼 잘라 넣으니 12/12 WAV로 나온다.
  머티리얼은 셰이더·텍스처 참조를 이름으로 풀어 JSON으로 낸다.
  폰트는 파일 매직으로 .ttf/.otf를 구분한다. Spine(`.atlas`/`.skel`)은 따로 담는다 —
  이 게임들의 애니메이션 실체는 Unity AnimationClip이 아니라 Spine이다.

- **`hierarchy` — 씬·프리팹 계층 복원** (새 모듈, 새 CLI 명령).
  빌드된 게임에는 .unity/.prefab 파일이 없다. 씬은 `level0`·`level1` SerializedFile로,
  Resources 프리팹은 `resources.assets` 안 GameObject 뭉치로 녹아 있다. 씬 전용
  오브젝트(RenderSettings 등)의 존재로 둘을 갈라내고, Transform 그래프를 따라
  트리를 만들어 JSON으로 낸다. 노드마다 이름·활성·RectTransform 기하·컴포넌트 목록이
  담기고, `typetree` 가 붙으면 컴포넌트 필드 값까지 들어간다
  (Image가 쓰는 스프라이트 이름, Text 문자열, 레이아웃 수치, 로컬라이즈 키).
  못 읽은 컴포넌트는 빼지 않고 `"fields": null` 로 남긴다 — "없다"와 "못 읽었다"는 다르다.

  실측: SheepNSheep 씬 2 · 프리팹 57 · 노드 1196 (11초) /
  ZenMatch 씬 3 · 프리팹 1013 · 노드 36143, MonoBehaviour 98.8%

### 같이 고친 것

- **`unity.load_bytes` 가 with 블록 본문의 예외를 삼켰다.** `yield` 가 try 안에 있어
  본문 예외가 폴백 경로로 흘러가 `RuntimeError: generator didn't stop after throw()` 로
  바꿔치기됐고, 호출자는 진짜 원인을 볼 수 없었다. load 호출만 감싸도록 고쳤다.
- **`ObjectIndex.expand` 가 tuple 안의 PPtr을 놓쳤다.** UnityPy는 머티리얼 프로퍼티
  맵을 `(이름, 값)` tuple로 주는데 list만 훑어서, 머티리얼 JSON의 텍스처 참조가
  통째로 안 풀렸다.
- **Shader 이름이 빈칸으로 나왔다.** Shader의 `m_Name`은 비어 있고 실이름은
  `m_ParsedForm.m_Name` 에 있다 (`Spine/Skeleton`, `UI/RoundMask`). `name_of` 가
  이 경우를 처리한다 — 머티리얼 JSON에서 "어떤 셰이더냐"가 가장 중요한 정보다.
- **이름이 겹치는 프리팹이 zip에서 서로를 덮어썼다.** ZenMatch에는 같은 이름의
  프리팹이 34쌍 있고, zipfile은 경고만 내고 둘 다 써서 한쪽이 사실상 사라졌다.
  `_2` 접미로 분리하고 건수를 로그·`index.json` 에 남긴다.
- **스트리밍 리소스를 이름이 안 맞을 때 아무 `.resource` 나 집어왔다.** 오프셋은
  원래 파일 기준이라 다른 파일에서 자르면 소리가 조용히 쓰레기가 된다. 이제 정확히
  맞는 파일만 쓰고, 후보가 딱 하나일 때만 대체하며 그 사실을 로그에 남긴다.
- 계층 JSON이 **항상 규격에 맞는 JSON**이 되게 했다. RectTransform에 NaN이 실제로
  들어 있고, json 기본 설정은 이걸 `NaN` 리터럴로 뱉어 뷰어의 `JSON.parse` 가
  통째로 실패한다.

테스트 146 → 241개. 새 모듈은 UnityPy 없이도 검증되도록 가짜 오브젝트 하네스
(`tests/helpers.py`)를 추가했다.

---

## 1.6.0 — 2026-08-13

뷰어 tiles 모드를 **게임 원본 타일 리소스로** 그리도록 확장.

- **타입별 타일 얼굴 이미지** — `viewer.tile_type_pattern: "card{}"` 를 주면 icon_dir을
  훑어 타입번호 → 이미지로 매핑하고, 타일 배경 위에 원본 비율을 지켜 얹는다.
  파일명을 직접 지정하는 `viewer.tile_type_images: {3: card3}` 도 지원.
  이전에는 base/ice/box 3장만 쓸 수 있어 타입은 숫자로만 표시됐다.
- **모달 하단 칩도 실제 타일 이미지로** 렌더된다(개수는 코너 배지). 칩 섹션 제목·설명은
  `viewer.queue_title` / `queue_hint` 로 게임에 맞게 바꿀 수 있다
  (기존 "슈터/엔티티 대기열 · 칸 안 숫자 = ammo" 문구가 다른 장르에도 나오던 문제).
- **렌더 크기 조절** — `viewer.tile_px` / `tile_px_modal`(타일 1개 최대 px),
  `card_px` / `modal_px`(렌더 영역), `tile_inset`(타일을 자기 칸보다 줄여 그리기).
  보드가 5~6타일 폭뿐인 게임은 카드에 꽉 맞추면 타일이 40px을 넘어 과하게 커지는데,
  `tile_px` 상한으로 다른 게임과 체감을 맞출 수 있다
  (Zen Match 중간값 24px ↔ SheepNSheep `tile_px: 26`). 코드 수정 없이 설정만 고치면 된다.
- 뱃지 아이콘을 `badges` 가 실제로 참조하는 것만 임베딩한다. 타일 이미지가 수십 장인
  게임에서 HTML에 같은 PNG가 두 번 실리던 낭비를 없앴다.

SheepNSheep 적용: 블록타입 id ↔ `cardN` 대응을 **il2cpp 메타데이터 문자열
`Textures/CardIcon/card{0}` 로 확정**(v1.5에서 추정으로 남겼던 미결). 타일 배경은 원본
`Card_BG`, 고정 블록은 실제 아이콘, 랜덤 배정 타일은 얼굴 없는 빈 타일로 그린다.

## 1.5.0 — 2026-08-13

**SheepNSheep 지원 추가** (세 번째 게임).

- **플러그인 훅 `split_levels(data, record)` 신설** — 소스 하나에 레벨이 여러 개
  들어있는 게임을 `[(세트명, 레벨id, 레벨 dict)]` 로 펼친다. 지금까지는 "파일/에셋 1개 =
  레벨 1개" 만 가능했다. decoded zip에도 펼쳐진 레벨별로 저장된다.
- `configs/sheepnsheep.yaml` + `levelscope/plugins/sheepnsheep.py` 추가.
  TextAsset 8개(`newMap1~3`·`newMapLv3`·`PvpMap`·`inviteMap`·`defaultMapData`·`AdventureMap`)
  → **스테이지 87개 / 맵 40개**. 최신형(`level`/`col`/`row`/`layer`)과
  구형(`level1,level2`/`rolNum`/`rowNum`/`layerNum`) 스키마를 함께 정규화하고,
  `AdventureMap` 의 easy/hard/hell 3그룹을 세트로 분리한다.
- 파악한 규칙: `blockTypeData = {블록타입id: 개수}` 이고 **합 × 3 = 랜덤 타일 수**
  (3개 맞추기). blockTypeData를 가진 73개 스테이지 전부에서 성립(`pool_ok` 컬럼으로 검증).
  `type` 0 = 랜덤 배정, 그 외 = 고정 블록.

검증: 에셋 8개 → 87 레벨, 디코딩 실패 0, xlsx(87행×20열, 수식 91개) · 뷰어 HTML(브라우저
콘솔 에러 0) · decoded zip 87개 · 스프라이트 37개(`card0~32`) 생성 확인.

또한 pandas·openpyxl·PyYAML을 설치해 **v1.4에서 미검증이었던 xlsx 경로를 실제 데이터로
확인**했다: PixelFlow Shooters 시트 160,313행 · Zen Match 스프라이트 79개 —
인수인계 문서 기준치와 정확히 일치.

---

## 1.4.0 — 2026-08-13

전면 리팩토링 + 추출 기능 보강. **설정 YAML과 산출물 형식은 그대로**라
기존 `configs/pixelflow.yaml`·`configs/zenmatch.yaml` 을 고칠 필요가 없다.

### 추출 기능 (새로 되는 것)

- **`.obb` 지원.** XAPK의 `Android/obb/*.obb` 안 레벨을 찾는다. 이전에는 최상위
  `.apk` 만 훑어서 obb 배포 게임은 아예 수집이 안 됐다. XAPK 해제 폴더의
  하위 폴더에 있는 obb도 찾는다(이전엔 최상위 `os.listdir` 만 봤다).
- **경로 설정 복수·glob 허용.** `levels_glob`, `input.unity.data_file` 이 문자열
  하나도 목록도 받는다. `data_file` 에 glob도 쓸 수 있어
  Addressables/split bundle(`assets/aa/Android/*.bundle`) 게임에 붙는다.
- **`input.merge_containers: true`** — 레벨이 base.apk와 obb에 나뉘어 있으면 합친다.
  (기본 false는 종전대로 "가장 많이 나온 컨테이너 하나만" 사용)
- **MonoBehaviour 레벨 수집.** `input.unity.type: MonoBehaviour` + `field`(선택,
  생략 시 가장 긴 문자열 프로퍼티) + `script_class`(선택). TextAsset이 아니라
  ScriptableObject에 레벨을 박는 게임에 대응.
- **XOR 키 자동 복구** — `detect --xor-scan`. 반복키 XOR을 known-plaintext(JSON 머리,
  압축 매직)·ASCII 문자 제약·열별 빈도·잔여 바이트 전수로 되찾아 바로 붙여 쓸
  `codec: ["xor:...", ...]` 을 출력한다. 복구 범위와 한계는 `xorscan.scan` 독스트링 참고.
- **진단 리포트.** 감지 실패 시 엔트로피·출력가능문자 비율·압축 매직으로
  "압축 vs 암호화"를 판정하고, IoC로 반복키 길이 후보를 추정해 보여준다.
- **실패 리포트 파일.** 디코딩 실패·수집 경고를 `out/<게임>_errors.txt` 로 남긴다.
  스프라이트 추출도 실패를 조용히 삼키지 않고 개수·사유를 보고한다.
- **`ls` 명령 추가** — `python -m levelscope ls --input <apk> [--glob 패턴]`.
  컨테이너 내부 경로를 훑어 `levels_glob` 을 잡을 때 쓴다.
- **`run --limit N`** — 앞 N개만 처리. 새 설정 빠른 확인용.

### 버그 수정

- **`detect --input <apk>` 가 실제로 동작한다.** 이전에는 apk 파일 자체를 샘플로
  읽어 "이진 데이터" 라고만 답했다(MANUAL 7장 온보딩 1단계가 무용지물이었다).
  이제 컨테이너 안에서 레벨 파일 후보를 골라 샘플링한다.
- **Windows에서 Unity TextAsset 수집이 크래시하던 문제.** UnityPy가 파일 핸들을
  쥔 상태로 `os.unlink` 를 호출해 `PermissionError [WinError 32]` 로 죽었다
  (Zen Match 경로 전체가 Windows에서 불가). 이제 bytes를 UnityPy에 직접 넘긴다.
- **한국어 Windows 콘솔(cp949)에서 로그 출력이 `UnicodeEncodeError` 로 죽던 문제.**
  CLI 시작 시 stdout/stderr을 UTF-8(errors=replace)로 재설정한다.
- **ZipFile 핸들 누수.** 후보 컨테이너를 열고 닫지 않았다. 이제 선택되지 않은
  컨테이너는 즉시 닫는다.
- **`sys.path` 영구 오염.** 플러그인 로드 시 넣은 경로를 되돌리지 않았다.
  컨텍스트 매니저로 임시 추가한다.
- **선택 의존 모듈 임포트 실패를 캐시하지 않아** 자동 감지가 같은 ImportError를
  수만 번 되풀이하던 문제 (XOR 전수 탐색에서 46초 → 2초).

### 구조 (리팩토링)

- **`container.py` 신설** — 폴더/zip/apk/xapk/obb/중첩 apk를 한 인터페이스로.
  전에는 `extract._from_zip`, `extract._collect_unity_textassets`,
  `sprites._iter_source_bytes` 가 같은 일을 서로 다르게 3번 구현했다.
- **`unity.py` 신설** — UnityPy 로드·오브젝트 순회·페이로드 추출 공통화.
  extract/palette/sprites의 임시파일 3중 중복 제거.
- **`xorscan.py` 신설** — XOR 복구 + 엔트로피/IoC 진단.
- **`decode.py` 코덱 레지스트리화.** `@step("이름")` / `@step("접두어:", prefix=True)`
  데코레이터로 등록. 새 코덱을 추가할 때 `apply_step` 의 if-체인을 고칠 필요가 없다.
- **`apply_chain(..., capture=True)`** 추가 — 구조 파싱 직전 평문을 같이 돌려준다.
  zip 원문 저장 때문에 체인을 두 번 돌리던 것을 한 번으로 (레벨 수천 개에서 디코딩 절반).
- **`cli.run` 분해** — 100줄 단일 함수를 `_decode_all` / `_build_dataframe` /
  `_write_error_report` 등으로 나눴다. pandas·openpyxl은 xlsx를 낼 때만,
  PyYAML은 설정을 읽을 때만 임포트한다(`detect`/`ls`/`sprites` 는 없이도 동작).
- **`LevelRecord` / `CollectResult` / `SpriteResult` 데이터클래스.** 구버전
  튜플 언팩(`for s, lid, raw, src in recs`, `recs, cont, path = collect_levels(...)`)도
  그대로 동작한다.
- **플러그인 중복 제거.** `plugins/` 와 `levelscope/plugins/` 에 같은 파일이
  두 벌 있어 "양쪽을 손으로 맞춰야" 했다. 패키지 안 한 벌로 정리하고
  `plugins/` 는 프로젝트 전용 확장 자리로 남겼다(`plugins/README.md`).

### 테스트

- `tests/` 신설 — 135개. **표준 라이브러리 unittest라 추가 설치 없이 돌아간다.**

      python -m unittest discover -s tests -t .

  코덱 전 스텝·중첩 체인·`hex ⊂ base64` 모호성, 컨테이너(obb·중첩 apk·해제 폴더),
  수집(복수 glob·병합·컨테이너 선택), 점 경로/보드, XOR 복구, CLI `run` 전체 흐름
  (합성 apk → 뷰어 HTML + decoded zip)을 덮는다.
- `tools/verify_baseline.py` — 실제 APK로 기준치를 대조하는 회귀 검증 스크립트.

### 검증

실제 데이터로 v1.3과 대조해 결과가 같음을 확인했다.

| 대상 | 결과 |
|---|---|
| PixelFlow (해제 폴더 / xapk 직접) | 레벨 2,384 · 8세트 · 메인 2,100 · isValid 2,100 · 디코딩 실패 0 · 팔레트 34색 — 전부 일치 |
| Zen Match (AAA.xapk) | TextAsset 4,502(main 4,493 + variant 9) · 디코딩 실패 0 · board(v2) 6 — 일치 |

Zen Match 포맷 분포는 main 기준 layered 3,705 / random 782 로, 인수인계 문서의
3,704 / 783 과 1개 차이가 난다. **v1.3을 같은 PC에서 돌려도 같은 값이 나오므로
리팩토링 때문이 아니다** — 이름이 중복된 레벨 쌍에서 어느 쪽이 `main` 이 되는지가
UnityPy 버전·플랫폼에 따라 갈린다(문서 수치는 Cowork/Linux 실행 결과).
합계·main/variant 분리·board 수는 완전히 일치한다.

---

## 1.3 — 2026-08-13 (Cowork 세션)

`levelscope_v1.3.zip`. Zen Match 지원 추가(Unity TextAsset 입력, tiles 렌더 모드,
스프라이트 추출). 변경 이력이 파일로 남아 있지 않아 1.0→1.3 사이의 세부는 복원 불가.

## 1.0 — 2026-08-13 (Cowork 세션)

PixelFlow 파이프라인 최초 구성 (extract/decode/schema/palette/report_xlsx/viewer/cli).
