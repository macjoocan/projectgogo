pandas>=2.0
openpyxl>=3.1
PyYAML>=6.0
UnityPy>=1.10
# hierarchy 의 컴포넌트 필드에 필요 (IL2CPP MonoBehaviour typetree 복원).
# 없으면 hierarchy 는 이름·타입까지만 내고 필드는 null 로 남는다 — 런은 죽지 않는다.
# .NET 8+ 런타임이 함께 필요하다.
TypeTreeGeneratorAPI>=0.0.10
# 선택: 해당 포맷의 게임에서만 필요
# zstandard
# lz4
# Brotli
# pycryptodome
# msgpack
